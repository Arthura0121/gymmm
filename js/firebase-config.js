/* ============================================================
   Optional: fill this in with your own free Firebase project to
   get REAL cross-device sync (your phone <-> your dad's phone).
   Leave it as-is and the app still works, but the dad panel will
   only update on the same browser/device. See README.md.
   ============================================================ */

window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  databaseURL: "https://YOUR_PROJECT-default-rtdb.firebaseio.com",
  projectId: "YOUR_PROJECT",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
